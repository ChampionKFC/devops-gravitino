apiVersion: v1
kind: Service
metadata:
  name: gravitino-frontend-service-dev
  labels:
    app: gravitino-frontend
spec:
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
  type: ClusterIP
  selector:
    app: gravitino-frontend
