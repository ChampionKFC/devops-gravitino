apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gravitino-frontend-ingress
  annotations:
    # Добавьте здесь необходимые аннотации для вашего Ingress контроллера
    # Например, если используете nginx-ingress:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
    - host: frontend.devopsim.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: gravitino-frontend-service-dev
                port:
                  number: 80
